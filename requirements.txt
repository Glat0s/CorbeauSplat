PyQt6==6.10.2
requests==2.32.5
urllib3==2.6.3
numpy==2.4.3
send2trash==2.1.0
opencv-python==4.13.0.92

# Upscale Dependencies (managed by setup_dependencies.py for the correct venv)
# gfpgan/facexlib/basicsr/realesrgan removed — using ONNX weights via custom vendor kernels
kornia==0.8.2
onnxruntime-gpu==1.24.3

# PyTorch cu129 — index URL and strategy configured in pyproject.toml [tool.uv]
# Install with: uv pip install -r requirements.txt
# Or use the install script: python -m app.scripts.setup_dependencies
torch==2.8.0+cu129
torchvision==0.23.0+cu129
# segment-anything is installed via the VR180 setup:
#   uv pip install git+https://github.com/facebookresearch/segment-anything.git
# triton-windows (torch.compile on Windows):
triton-windows==3.6.0.post26
